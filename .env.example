# Copy to .env (never committed). Only the worker pool and paid opt-in tests read these.
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_VERSION=
AZURE_OPENAI_BASE_URL=
# deployment-name map, e.g. gpt-4.1-mini=my-deployment,gpt-5.4-mini=my-other-deployment
AZURE_OPENAI_DEPLOYMENT_NAME_MAP=
JEV_API_KEY=
DAYTONA_API_KEY=
